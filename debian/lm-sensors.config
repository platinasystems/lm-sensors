#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

if [ "$1" = configure -o "$1" = reconfigure ] ; then
	if dpkg --compare-versions "$2" lt-nl 2.6.3-1; then
		db_input high lm-sensors/negatives || true
		db_go
	fi
fi

exit 0
