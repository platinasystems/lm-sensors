#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

# If we're not in a devfs world, ask about creating devices.
if [ ! -c /dev/.devfsd ]; then
  db_input high lm-sensors/create-devices || true
  db_go
fi
